{
  "name": "userscripts-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=24.0.0"
  },
  "volta": {
    "node": "24.14.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "pnpm -r build",
    "prepare-pages": "node scripts/prepare-pages.mjs",
    "prepare-tampermonkey-import": "python3 scripts/prepare_tampermonkey_import.py",
    "type-check": "pnpm -r type-check",
    "test": "node --test",
    "validate": "node scripts/inspect-workspace.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.0.11",
    "vite-plugin-monkey": "^8.0.0"
  }
}
